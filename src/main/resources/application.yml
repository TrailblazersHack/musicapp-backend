server:
  port: 8080

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/musicapp_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
springdoc:
  swagger-ui:

jwt:
  secret: 123123
