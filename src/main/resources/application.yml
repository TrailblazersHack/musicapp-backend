server:
  port: 8080

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/musicapp-db
    username: postgres
    password: dreges228
  jpa:
    hibernate:
      ddl-auto: update